<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>Faclabprojet0</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width" />
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' , :recursive => true %>
    <%= javascript_include_tag 'application',media: 'all', 'data-turbolinks-track': 'reload' , :recursive => true %>
    <script src="http://spin.js.org/spin.min.js"></script>
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/4.1.2/papaparse.min.js"></script>
  </head>
  <body>
    <header>
        <nav class="subnav">
            <ul>
                <li><%= link_to 'connexion' , root_path, :class => current_class?(root_path) %></li>
                <li><%= link_to 'places' , places_path, :class => current_class?(places_path)  %></li>
                <li><%= link_to 'visits' , visits_path, :class => current_class?(visits_path)  %></li>
                <li></li>
            </ul>
        </nav>
    </header>
    <main>
        <section>
          <article id="messageAlerte">
            <p><% flash.each do |message_type, message| %>
              <%= message %></p>
            <% end %>
          </article>
            <%= yield %>
        </section>
  </body>
</html>
